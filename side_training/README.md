# 个人训练模式

该目录是个人训练模式的公开集成边界。产品完整版由私有运行时完成侧视球路追踪、训练事件统计、人体姿态特征提取和动作时序分析；公开工程通过`runtime_bridge.py`启动对应运行器，并将结果交给触控界面和云端适配层。

私有运行时应接受：

```text
--video <path> [--cloud]
```

输出文件可包括训练标注视频、轨迹记录和动作摘要，具体字段通过`contracts/task-result.schema.json`对外保持稳定。模型、特征构造、动作分段和训练量化实现不属于公开仓库。
