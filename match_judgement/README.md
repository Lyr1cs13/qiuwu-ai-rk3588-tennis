# 双人对打模式

该目录是双人对打模式的公开集成边界。产品完整版由私有运行时完成比赛视角追踪、球场定位、事件分析、辅助判罚和比分更新；公开工程通过`runtime_bridge.py`启动对应运行器，并读取标准化结果用于界面展示与可选上云。

私有运行时应接受：

```text
--video <path> [--cloud] [--calibrate-only]
```

输出文件可包括标注视频、轨迹数据、事件记录和判罚摘要，具体字段通过`contracts/task-result.schema.json`对外保持稳定。模型、阈值、映射和判罚实现不属于公开仓库。
